[build]
  publish = "public"          # Ensure this is the directory where Gatsby outputs your build files
  functions = "netlify/functions" # Ensure this is the directory where your Netlify functions are located

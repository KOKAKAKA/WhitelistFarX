[build]
  functions = "netlify/functions"
  publish = "public"  # Ensure this matches your Gatsby build output directory

[context.production.environment]
  NODE_VERSION = "16"  # Ensure this matches your Node.js version
